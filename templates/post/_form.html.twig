{{ form_start(postform) }}
<div class="row">
    <div class="col-12">
        {{ form_widget(postform.content) }}
    </div>
</div>
<div class="row">
    <div class="col-12 d-flex justify-content-end">
        <button class="btn btn-primary rounded-pill my-2">{{ button_label|default('Tweeter') }}</button>
    </div>
</div>
{{ form_end(postform) }}
